[project]
name = "ifc-geometry-playground"
version = "0.1.0"
description = "Python tooling for IFC schema export experiments"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ifcopenshell>=0.8.4.post1",
]

[tool.uv]
package = false
